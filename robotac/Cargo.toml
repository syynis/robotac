[package]
name = "robotac"
version = "0.1.0"
edition = "2021"

[dependencies]
tac-types = {path = "../types"}
enum-map = {workspace = true}
rand = {workspace = true}
itertools = {workspace = true}

[features]
